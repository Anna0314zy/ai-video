import { Layout } from 'antd'
const style: React.CSSProperties = {
  backgroundColor: '#F2F3F7',
}
const ChatContent = () => {
  return <Layout.Content style={style}>图片</Layout.Content>
}
export default ChatContent
